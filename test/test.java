public class test {
    public void run()
    {
        Artifact jar = new DefaultArtifact(
          root.getGroupId(),
          root.getArtifactId(),
          "jar",
          root.getVersion());
    }
}
